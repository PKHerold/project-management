{
  "name": "project-manager",
  "version": "1.0.0",
  "description": "Project Management Tool with Role-Based Access Control",
  "main": "src/index.js",
  "scripts": {
    "start": "npx prisma generate && npx prisma db push && npm run build && node src/index.js",
    "dev": "nodemon src/index.js",
    "build": "cd client && npm run build",
    "postinstall": "npx prisma generate && npm install --prefix client"
  },
  "dependencies": {
    "@prisma/client": "^6.6.0",
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "jsonwebtoken": "^9.0.2"
  },
  "devDependencies": {
    "prisma": "^6.6.0",
    "nodemon": "^3.1.9"
  }
}
